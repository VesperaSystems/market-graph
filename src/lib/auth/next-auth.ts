// NextAuth/Drizzle production integration seam.
// The NextAuth beta used by mission-control currently declares peer support for Next 14/15,
// so the Next 16 MVP uses local session scaffolding until a supported auth adapter is selected.
export const nextAuthIntegrationStatus = "pending-next16-compatible-adapter";
